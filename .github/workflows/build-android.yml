name: Build Android
on: { workflow_dispatch: {}, push: { tags: ['kicdn-apk-v*'] } }
permissions: { contents: write }
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "TODO build kicdn APK (model on Jarvis-android workflow)"

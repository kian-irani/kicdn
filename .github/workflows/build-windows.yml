name: Build Windows
on: { workflow_dispatch: {}, push: { tags: ['kicdn-win-v*'] } }
permissions: { contents: write }
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.11' }
      - run: echo "TODO build kicdn Windows (model on kian_v2ray build-kv2m.yml — PySide6 + PyInstaller)"
